Simple command line Python script for downloading a images from a 4chan thread

Uses Beutifulsoup4 for parsing and httplib for https connections (does not verify certificate)

TODO:
 * Display progress (e.g. overall speed rather than per thread)
 * Handle timeouts and retry downloads
 * Handle CTRL+C in threads correctly
 * Check command line arguments